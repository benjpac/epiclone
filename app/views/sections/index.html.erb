<h1>title e.g. rails</h1>

<ul>
  <% @sections.each do |section| %>
    <li><%= link_to section.name, section_path(section) %></li>
  <% end %>
</ul>


<hr>

<%= link_to "New Section", new_section_path %>